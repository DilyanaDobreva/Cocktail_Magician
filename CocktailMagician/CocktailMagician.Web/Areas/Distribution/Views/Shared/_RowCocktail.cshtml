@model CocktailInListViewModel


    <div class="col-md-3 " align="center">
        <div class="card-deck margin-card shadow">
            <h5 class="card-title" >@Model.Name</h5>
            <img class="card-img-top image-cocktail-index" src="@Model.ImageURL" alt="Card image cap">
            <div class="card-body">
                <div>
                    <a asp-action="Details" asp-route-id="@Model.Id" >Show Details</a>
                </div>
                <a asp-action="CockTailReview" asp-route-id="@Model.Id">Add Review</a>
            </div>
        </div>
    </div>

